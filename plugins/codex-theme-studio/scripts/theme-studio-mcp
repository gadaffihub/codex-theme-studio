#!/bin/zsh
set -eu

plugin_root="${0:A:h:h}"
python_bin="${PYTHON:-$(command -v python3 2>/dev/null || true)}"
[[ -n "${python_bin}" ]] || { print -u2 "Theme Studio needs Python 3."; exit 127; }
exec "${python_bin}" "${plugin_root}/theme_studio_mcp.py"
